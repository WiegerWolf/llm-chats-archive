services:
  archive:
    build:
      context: .
    ports:
      - "8080:8000"
    environment:
      ARCHIVE_DATA_DIR: /data
      ARCHIVE_CORS_ORIGINS: http://localhost:5173,http://127.0.0.1:5173
    volumes:
      - archive-data:/data

volumes:
  archive-data:
